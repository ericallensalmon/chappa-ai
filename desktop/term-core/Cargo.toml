[package]
name = "term-core"
version = "0.1.0"
edition = "2021"

[dependencies]
alacritty_terminal = "0.26.0"
memchr = "2"
portable-pty = "0.9.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
